import ColorSlider from "./ColorSlider"
const ColorSliders = () => {
  return (
    <section className="color-sliders">
        <ColorSlider />
        <ColorSlider />
        <ColorSlider />
    </section>
  )
}

export default ColorSliders